# Contributing

`npm run dev` points `~/.pi/agent/settings.json` at this checkout and disables the published package. It backs the file up first. Restart pi to pick up edits. `npm run dev -- --off` restores.
